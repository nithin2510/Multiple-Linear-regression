# Multiple-Linear-regression
Multiple Linear Regression (MLR) is used to predict a dependent variable using multiple independent variables. In this project, data is preprocessed, split into training and testing sets, and the model is trained. Performance is evaluated using MSE and R², and predictions are compared with actual values.
